# Station identity — must match a row in CCTV Master config.yaml (offices / installations)
INSTALLATION_ID=INST-003
OFFICE_CODE=CJ-002
OFFICE_NAME=Central Jail

# CCTV Master sync API (HQ server)
MASTER_API_URL=http://192.168.13.157:8000/api/sync
SYNC_API_KEY=cctv-sync-key-change-me
SYNC_PUSH_INTERVAL_MINUTES=2
SYNC_ENABLED=false

# Set to true when master is reachable:
# SYNC_ENABLED=true

# Database (example)
# DATABASE_URL=postgresql+asyncpg://postgres:password123@localhost:5432/surveillance
